from setuptools import setup

setup(
   name='rssFlightManagementServer',
   version='0.0.1',
   description='Aberdeen University Rocketry Society Flight management server',
   author='Sidney Pauly',
   author_email='rocketry@ausa.org.uk',
   packages=[],  
   install_requires=[], 
   test_suite="tests",
)